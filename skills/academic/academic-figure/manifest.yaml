name: academic-figure
description: >
  Declarative routing manifest for academic data/result figure work. Data plots
  use Python-generated editable SVG with CS/AI/ML chart design gates. Model
  framework diagrams, architecture diagrams, overview diagrams, and complex
  mechanism schematics are outside automatic drawing scope. Deeper references
  load only when needed.

always_load:
  - static/core/stance.md
  - static/core/red-lines.md
  - static/core/output-contract.md
  - static/core/anti-patterns.md
  - ../shared/core/non-invention-rules.md

axes:
  mode:
    detect: |
      Select the smallest mode that satisfies the request. Use an explicit user
      mode if provided. Data files or numeric results imply chart-from-data.
      Existing figure files plus critique language imply figure-audit; existing
      figures plus requested edits imply figure-revision. Section-level figure
      planning implies figure-blueprint. Requests to draw model framework,
      architecture, overview, or complex mechanism diagrams route to
      figure-blueprint only for manual_figure_needed notes; never render images,
      SVG, or prompts.
    values:
      chart-from-data: references/workflow-chart-from-data.md
      figure-blueprint: references/figure-contract.md
      figure-audit: references/qa-contract.md
      figure-revision: references/qa-contract.md
    multi: false

references:
  on_demand:
    - condition: build a Figure Contract
      path: references/figure-contract.md
    - condition: execute Python data plotting workflow
      path: references/workflow-chart-from-data.md
    - condition: write Python plotting code
      path: references/api.md
    - condition: select chart type
      path: references/chart-types.md
    - condition: final delivery, audit, or revision QA
      path: references/qa-contract.md
    - condition: color, typography, layout, and export rationale
      path: references/design-theory.md
    - condition: CS/AI/ML chart design pattern, dense multi-panel layout, benchmark, ablation, training, robustness, or efficiency figure
      path: references/cs-ai-chart-design-patterns.md
    - condition: user needs an end-to-end example
      path: references/tutorials.md
