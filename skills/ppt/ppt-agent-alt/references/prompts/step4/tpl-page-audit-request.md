# Step 4 人工审计返工请求

> 本文件用于把用户在 Step 4 的人工审计意见固化成可校验合同。
> 主 agent 必须先生成它，再跑 `contract_validator.py page-audit-request`，通过后才能创建 `PagePatchAgent`。

page_number: {{PAGE_NUM}}
start_stage: {{START_STAGE}}
end_stage: {{END_STAGE}}
user_request: {{USER_AUDIT_REQUEST}}
target_asset_path: {{TARGET_ASSET_PATH}}
runtime_context_paths: {{RUNTIME_CONTEXT_PATHS}}
planning_output: {{PLANNING_OUTPUT}}
html_output: {{SLIDE_OUTPUT}}
png_output: {{PNG_OUTPUT}}
