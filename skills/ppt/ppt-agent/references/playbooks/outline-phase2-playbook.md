# Outline Phase 2 Playbook -- 严格自审与修复闭环

## 目标

大纲编写完成后，立即切换到审查者（QA/质量门卫）视角。对上一阶段生成的 `outline.txt` 及其逻辑合理性进行逐项对账。

---

## 严审规则与态度（拒绝自以为是的敷衍！）

- **绝不轻易放行**：禁止看一遍之后就写“经过检查，逻辑清晰，顺利通过”这种自命不凡的废话！你是一个鸡蛋里挑骨头的恶毒 QA！不要自己骗自己。
- **必须找到痛点**：第一遍看下去，如果觉得“都挺好的”，那就说明你根本没在审查。去看看哪些页的目标像套话？哪些素材根本撑不起野心？如果没有挑出至少一两个刺并进行真实的修改，你的审查就是失职。
- **直接在原文件上修改**：不要另起炉灶写一份，如果发现某页不达标，直接修改原 `outline.txt` 的那一行。
- **最多 2 轮修正循环**：发现问题 -> 爆改 -> 重新整体审视。

---

## 自审检查清单（9项门禁）

必须在脑海里（或推演日志中）走完这 9 项检查，任何一项不通过都不允许交卷：

| # | 检查项 | 严苛标准 | 不通过的处理方式 |
|---|--------|------|-------------|
| 1 | **页数对齐** | 大纲内包含的页面总数（不算 Part 标题这种虚页，算 `第 X 页`），必须严格等于需求中要求的目标页数。 | 裁撤水分页 / 扩充干瘪页 |
| 2 | **Part 规模** | 每个 Part (除了首尾 cover/cta外) 必须 >= 2 页支撑。 | 如果只有 1 页，说明这不是一个独立的 Part，合并到别处或扩充。 |
| 3 | **Part 逻辑** | Part 之间有明确的逻辑纽带（如递进、果因），绝不能是像流水账一样的同级主题并列。 | 调整 Part 写作顺序或重写 Part 目标。 |
| 4 | **数据诚实度** | `素材来源` 如果标了 `true`，你在 Brief 中真的能找到吗？ | 找不到必须标 `false` 并说明缺口在哪。 |
| 5 | **单页单聚焦(致命门禁)** | 每页的 `页目标` 必须是一句话。**绝对不能包含"和"、"与"这类连词！** 一页试图讲两件事，排版必毁。 | 拆成上下两页讲。 |
| 6 | **密度合同完整性** | 大纲头部必须写明 `密度倾向` 与 `密度曲线`；每页必须有 `密度下限 / 密度目标 / 密度上限 / 节奏动作 / 信息姿态 / 锚点类型`。 | 补齐缺失字段，禁止把密度判断留给后续阶段临场发挥。 |
| 7 | **密度窗口合法性** | 每页必须满足 `下限 <= 目标 <= 上限`；`cover / section / end` 不得是 `dashboard`；`ultra_dense` 模式下内容缓冲页也不能退回 `low`。 | 重写对应页面的密度区间。 |
| 8 | **叙事弧线** | 不是每一页全是干巴巴的数据。有 `close` 沉淀认知吗？有 `section` 制造呼吸吗？叙事角色（evidence/comparison/process/close/cta）的组合是否有节奏起伏？ | 调整页面“叙事角色”组合的密度分配。 |
| 9 | **灵魂论点** | 整套 PPT 必须有一个一锤定音的 core_thesis，不能是一句正确的废话。 | 提纯论点。 |

### 密度专项铁律

- 禁止连续 3 页 `high / dashboard`
- `dashboard` 前后必须至少有 1 页非 `dashboard` 过渡
- `relaxed` 模式禁止出现 `dashboard`
- `balanced` 模式允许 `dashboard`，但只能少量出现

---

## FINALIZE 签名契约

只有在 7 项检查全部自审通过后，才允许在文件末尾追加签名并宣布完成任务。
签名格式如下（追加在文件最末尾）：

```text
---
SELF_REVIEW_PASS
自审轮数：{你实际修改审查的次数}
自审时间：{YYYY-MM-DD HH:MM}
```
