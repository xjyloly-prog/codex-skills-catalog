# 长期兴趣问题匹配

输入：`search_results`、`twelve_problems`。输出：`matched_problem_ids`、`match_reason`。

将搜索结果与用户配置的开放式长期问题做语义匹配。只标注有清晰概念、因果或应用关联的项目，不因共享一个宽泛关键词就判定匹配。

问题清单为空时返回空匹配，不影响主查询。兴趣问题没有截止日期，也不是待办事项；已有明确研究任务时优先服从任务范围。
