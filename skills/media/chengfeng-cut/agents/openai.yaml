interface:
  display_name: "chengfeng · 剪口播"
  short_description: "从逐词转录、修字到删词判断，产出一份已人工复核的删词账本"
  default_prompt: "使用 $chengfeng-videocut:chengfeng-cut 处理这条口播视频：转录、按词典修字、判断口误与重复，并打开 Studio 让用户复核删词。"
policy:
  allow_implicit_invocation: true
